---
export interface LogoItem {
  src: string;
  alt: string;
}

interface Props {
  logos: LogoItem[];
}

const { logos } = Astro.props as Props;
---

<!-- Static grid on small viewports; scrolling marquee from md breakpoint up -->
<ul
  class="logo-marquee-gallery m-0 list-none grid grid-cols-2 gap-4 p-6 sm:grid-cols-3 sm:gap-6 md:hidden"
  aria-label="Partner logos"
>
  {logos.map((logo) => (
    <li class="flex aspect-square items-center justify-center rounded-lg border border-gray-100 bg-white/80 p-3 sm:p-4">
      <img
        src={logo.src}
        alt={logo.alt}
        class="h-full w-full max-h-full max-w-full object-contain object-center"
        loading="lazy"
        decoding="async"
      />
    </li>
  ))}
</ul>

<div class="logo-marquee hidden md:block" aria-label="Partner logos marquee">
  <div class="logo-marquee-track">
    <ul class="logo-marquee-group">
      {logos.map((logo) => (
        <li class="logo-marquee-item">
          <img src={logo.src} alt={logo.alt} loading="eager" decoding="async" />
        </li>
      ))}
    </ul>
    <ul class="logo-marquee-group" aria-hidden="true">
      {logos.map((logo) => (
        <li class="logo-marquee-item">
          <img src={logo.src} alt="" loading="eager" decoding="async" />
        </li>
      ))}
    </ul>
  </div>
</div>

<style>
  .logo-marquee {
    overflow: hidden;
    width: 100%;
  }

  .logo-marquee-track {
    display: flex;
    width: max-content;
    animation: logo-marquee-scroll 28s linear infinite;
    will-change: transform;
    transform: translate3d(0, 0, 0);
    backface-visibility: hidden;
  }

  .logo-marquee-group {
    display: flex;
    flex: 0 0 100%;
    width: 100%;
    min-width: 100%;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .logo-marquee-item {
    flex: 0 0 25%;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1.25rem 1rem;
  }

  .logo-marquee-item img {
    max-height: 58px;
    width: auto;
    object-fit: contain;
    transform: translateZ(0);
  }

  @media (max-width: 1023px) {
    .logo-marquee-item {
      flex-basis: 33.3333%;
    }
  }

  @media (max-width: 767px) {
    .logo-marquee-item {
      flex-basis: 50%;
    }

    .logo-marquee-item img {
      max-height: 50px;
    }
  }

  @keyframes logo-marquee-scroll {
    from { transform: translate3d(0, 0, 0); }
    to { transform: translate3d(-50%, 0, 0); }
  }

  @media (prefers-reduced-motion: reduce) {
    .logo-marquee-track {
      animation: none;
      width: 100%;
      display: grid;
      grid-template-columns: 1fr;
    }

    .logo-marquee-group[aria-hidden="true"] {
      display: none;
    }

    .logo-marquee-group {
      width: 100%;
      min-width: 100%;
    }
  }
</style>
